# 드래곤로드 (01038088) 데이터 내려받기

KTF WIPI-C `Clet`, Thumb, 이미지 베이스 `0x100000`이므로
`파일오프셋 = va - 0x100000`. 아카이브는 `P/dragon.*`로 받아둔 데이터를
이미 들고 있다.

## 먼저 읽을 것 — 실은 내려받을 필요가 없었다

타이틀은 다운로드를 권하기 **전에** 자기 데이터 파일이 있는지 슬롯 5
(`db_stat_by_name`)로 여섯 번 묻는다: `dragon.map`, `dragon.res`,
`script.csd`, `dragon.spr`, `dragon.dat`, `dragon.vlu`. 우리 슬롯 5는
아카이브가 싣고 온 저장소를 "없음(-22)"이라고 답하고 있었다 — 같은 것을
`MC_dbExists`도 `MC_dbOpenDataBase`도 "있다"고 답하는데 혼자서만.

그 한 줄을 고치니 다운로드 화면 자체가 나오지 않고 타이틀 화면으로 바로
들어간다(두 아카이브 모두). **그리고 이 길로 가야 한다** — 다운로드를 끝까지
받아도 타이틀은 `모든 업데이트를 완료하였습니다. 게임 종료 후 다시
시작해주세요`를 띄우고 확인을 누르면 **앱을 끝낸다.** 받은 것이 다음 실행에
남지 않으면 그대로 무한 반복이다.

아래는 그래도 다운로드를 요구하는 경우(아카이브에 데이터가 반만 있다든지)를
위해 읽어둔 프로토콜이고, `wie_backend::local_network::funter`가 그것을
답한다. 11개 파일 전부를 끝까지 내려주는 것까지 확인했다.

## 한 줄 요약

`[드래곤로드 정보] … 데이터파일을 다운받습니다` 화면에서 **1(예)를 눌러도
소켓 하나 열리지 않았다.** 타이틀은 그 전에 net 테이블 **슬롯 34**를 부르고,
답이 음수면 거기서 그만둔다. 우리 테이블은 33칸까지였으므로 그 슬롯은
`-1`로 거절되고 있었다. 밖에서 보이는 것은 `MC_netSetReadCB(0, ...)`가 몇 초에
한 번씩 반복되는 것뿐 — **열린 적이 없는 소켓**에.

## 슬롯 34 (`0x1072b8`)

```asm
1072c4  adds r5, r0      ; r5 = 세션 객체
1072c6  lsls r4, r2,#16  ; (short)r2 = 포트
1072d4  strh r4, [r5,#0x18]
1072d6  ldr  r4, [r3]    ; net 인터페이스 테이블
1072da  adds r3, #0x88   ; 0x88/4 = 34
1072dc  ldr  r0, [r5,#0xc]   ; "kt68wipiwicgs.magicn.com:27090"
1072e0  bl   #0x153668       ; bx r3 트램펄린
1072e4  cmp  r0, #0
1072e6  blt  #0x10731e       ; 음수면 포기 (소켓도 만들지 않는다)
...
1072fa  ldr  r3, [r2]        ; 테이블[0] = MC_netConnect
1072fc  bl   #0x153668       ; connect(콜백, 세션)
107304  cmp  r0, -10 / -7    ; 둘 다 "진행 중"
```

인자는 **r0에 `호스트:포트` 문자열 하나뿐**이다(r1/r2는 직전 호출이 남긴
찌꺼기). 답은 부호만 본다 — 아무것도 보관하지 않는다. 답을 주면 타이틀은
평범한 순서를 밟는다: `MC_netSocket(2, 1)` → 슬롯 30 →
`MC_netSocketConnect(1, 211.115.203.17, 15102)`. 즉 **열기 전에 목적지를 두고
묻는 질문**이지 연결이 아니다. `wie_wipi_c::api::net::check_server` 참고.

문자열 두 개가 바이너리에 나란히 있다 (`0x155030`, `0x155060`).

```
0x155030  kt68wipiwicgs.magicn.com:27091
0x155050  TEST_BILLSOCK
0x155060  kt68wipiwicgs.magicn.com:27090
```

`TEST_BILLSOCK`은 데몬헌터가 슬롯 30에 넘기던 이름과 같은 것이다.

## 프레이밍 (`0x107654`, `0x107770`)

수신은 두 단계다. 헤더 8바이트를 읽고, 그다음 `길이 - 8`을 읽는다.

```asm
1076f4  ldr  r1, [pc,#0x6c]  ; 0x84c  — 매직이 놓이는 자리
1076f8  ldrh r3, [r3]
1076fa  ldr  r2, [pc,#0x6c]  ; 0xffff — 매직 상수
1076fc  cmp  r3, r2
1076fe  bne  ...             ; 아니면 오류 7로 끊는다
10770c  ldr  r3, [pc,#0x5c]  ; 0x848  — 전체 길이가 놓이는 자리
107714  subs r1, #8          ; 본문 = 길이 - 8
```

그러므로 프레임은

```
+0  u32 LE  전체 길이 (헤더 8바이트 포함)
+4  u16     0xffff      ← 반드시 이 값
+6  u16     메시지 id
+8  본문
```

읽기 루프(`0x107770`)는 한 번에 최대 `0x800`바이트씩 슬롯 32로 받아
`객체+0x48` 버퍼에 모으고, 완성된 프레임을 큐에 넣는다(`0x1075e0`).
게임 쪽 층이 그 큐를 꺼내 쓴다.

첫 요청은 20바이트다.

```
14 00 00 00  ff ff 40 06  00 00 02 01  01 01 00 00  00 f0 00 00
└ 길이 20 ┘  └매직┘└id ┘  └──────────── 본문 12 ────────────┘
```

id는 `0x0640` = 1600.

## 보내고 무엇을 기다리는지 (`0x13baa8`)

요청 한 건은 이 함수를 거친다.

```asm
13baa8  ; f(객체, 요청기술자, 기다릴_id, 핸들러, …, 타임아웃)
13bac2  ldrh r1, [r4]        ; 기술자의 id
13bac8  bl   #0x107440       ; 전송 (위 프레임을 만든다)
13bacc  movs r3, #2
13bace  str  r3, [r5,#0x28]  ; 상태 = 응답 대기
13bad2  str  r6, [r5,#0x38]  ; 기다릴 id   ←
13bad4  str  r3, [r5,#0x3c]  ; 핸들러      ←
```

받는 쪽(`0x13b880`)은 도착한 프레임의 id를 `[객체+0x38]`과 **그대로 비교**하고,
다르면 `0x13ba4c`로 세션을 접는다. 요청 id를 그대로 되돌려주면 안 되는 이유가
이것이다 — 우리 ack가 처음에 그렇게 답해서 타이틀이 콜백을 내리고 끊었다.

기술자는 `{u16 id, 시작, 끝}`이고(`0x13b368`), 본문은 `0x13b370`으로 덧붙인다.

## 메시지들

| 보냄 | 기다림 | 무엇 |
|---|---|---|
| 1600 (`0x640`) | 1601 | 받을 파일 목록 요청 |
| 1602 (`0x642`) | 1603 | 파일 하나를 오프셋부터 |
| 1604 (`0x644`) | 1605 | 같은 파일의 그다음 조각, 오프셋 4바이트뿐 |
| 30101 (`0x7595`) | — | 심장박동, 답을 기다리지 않는다 |

1600의 본문은 `0x13d4cc`가 만든다: `u32 0x01020000` 다음 객체의 8바이트.
포착한 것과 정확히 같다.

## 1601 본문 (`0x13d53c`)

```
+0   u32 status        0이 아니면 "[다운로드 오류] 서버응답 오류" 화면
+4   u32 a             상위 16비트만 보관
+8   u32 b
+12  u32 count         뒤따르는 레코드 수
+16  count × 28바이트 레코드
```

레코드는 `0x113d74`가 읽는다.

```
+0   u32 도장 — 로컬에 있는 것과 비교한다
+4   u8  종류 (하위 7비트) — 0이면 내려받기, 2면 이름만 쓰고 상태 4
                최상위 비트가 서야 "이미 있으면 건너뛰기"를 본다
+6   u8  플래그 (지워진다)
+8   u32 크기
+12  char 이름[16]      ; dragon.vlu, script.csd, FUNTER_DL …
```

`count <= 0`이면 타이틀은 죽지 않고 심장박동만 계속 보낸다.

## 여기까지 실제로 확인한 것

프로브의 ack 엔드포인트로 손수 만든 답을 물려 본 결과다.

```
WIE_LOCAL_NET_ACK="any,len=u32le,type=4,id=ffff4106,status=<본문 hex>"
```

- id를 그대로 되돌려주면(1600) → 프레임은 받아들이지만 세션을 접는다.
- id 1601 + 본문 4바이트 → 1601 핸들러 안에서 레코드 28바이트를 읽다 죽는다
  (`memcpy(dst, 0, 0x1c)`), 즉 프레임은 옳고 본문이 모자란 것이다.
- id 1601 + `status=0, count=0` → 죽지 않고 30101 심장박동을 0.9초마다 보낸다.
- id 1601 + `count=1`, 레코드 `{크기 128, 이름 "dragon.vlu"}` → 타이틀이
  **`MC_dbOpenDataBase("dragon.vlu", 4, 1)`로 받을 자리를 만들고**, 이어서

  ```
  28000000 ffff4206 00000000 <그 28바이트 레코드>
  ```

  를 보낸다. 곧 **1602 = "이 파일을 이 오프셋부터 달라"**이고, 앞의 4바이트가
  오프셋이다(`0x13d5d8`).
- status를 0이 아닌 값으로 두면 "[다운로드 오류] … 다시 시도할까요?"가 뜬다.

## 1603 / 1605 본문 (`0x13d694`)

두 답은 **같은 핸들러**가 받는다. 1602가 기다리는 id는 0x0643(1603),
1604가 기다리는 id는 0x0645(1605)인데, 요청을 만드는 `0x13d5d8`과
`0x13d63c`가 넘기는 핸들러 주소는 GOT의 같은 칸(`+0x940`)이다. 그러니
본문 모양도 하나다.

```asm
13d69c  bl   #0x13b388       ; [sp+4] = u32 상태, [sp+8] = 그다음을 가리킴
13d6a2  cmp  r3, #0          ; 상태
13d6a6  cmp  r3, #0x83       ; 0x83이면 콜백 6 (파일 끝)
13d6aa  ... movs r1, #5      ; 그 밖의 0 아님이면 콜백 5 (오류)
13d6c2  bl   #0x14e3c4       ; 길이 4바이트를 복사
13d6ce  bl   #0x113ec8       ; (객체, 길이, 자료)
```

```
+0  u32  상태 (0 정상, 0x83 끝, 그 밖은 오류)
+4  u32  길이
+8  그 길이만큼의 바이트
```

## 한 조각을 받은 뒤 (`0x113ec8`)

```asm
113ed2  bl   #0x103eb8       ; db에 쓴다 (핸들은 [객체+0x24])
113ed6  ldr  r3, [r5,#0x3c]
113ed8  adds r4, r3, r4      ; 받은 누계 += 길이
113ee4  ldr  r2, [r5,#0x38]  ; 지금 파일의 번호
113ee6  movs r3, #0x1c
113ee8  muls r3, r2, r3
113eec  adds r3, r3, r1      ; 레코드 배열 [객체+0x44]
113eee  ldr  r3, [r3,#8]     ; 레코드의 크기
113ef0  cmp  r4, r3
113ef2  blo  #0x113efc       ; 아직 모자라면
113efc  bl   #0x13d63c       ; 이어받기 요청
113ef4  bl   #0x113f08       ; 다 받았으면 닫고 다음 파일
```

**한 답은 한 조각이지 한 파일이 아니다.** 모자라면 타이틀이
`0x13d63c(세션, 누계)`로 다시 묻는데, 이것이 **1604**다. 본문은 오프셋
4바이트뿐 — 파일 이름이 없다. 이미 진행 중인 그 파일이기 때문이다.

처음에 1604를 "다 받았다"는 뜻으로 읽고 1605에 4바이트만 실어 보냈더니,
타이틀은 그 4바이트를 상태로 읽고 길이 자리를 초기화되지 않은 스택에서
가져와 `db.stream_write(핸들, ..., 3735928559)`를 불렀다. 파일마다 조각
하나씩만 받고 다음으로 넘어가던 것이 이것이다.

## 레코드를 다시 (`0x113ce0`, `0x113d74`)

```
+0   u32  도장 — 로컬에 있는 것과 비교한다
+4   u8   종류: 하위 7비트(0이면 받는다), **최상위 비트가 "이미 있으면 건너뛰기"**
+8   u32  크기
+12  char 이름[16]
```

최상위 비트가 서 있을 때만 `0x113ce0`이 로컬을 들여다본다. 도장과 크기가
둘 다 맞으면 4(가지고 있음)를 돌려주고 한 바이트도 받지 않으며, 크기만
다르면 3(새로 받기), 로컬이 반쯤 있으면 6(이어받기)이다. 비트를 지워두면
언제나 3이므로 우리 목록은 늘 처음부터 내려준다.

`0x113d30(객체, 이미_있는_양, 크기)`는 `MC_fsAvailable`을 보고
`크기 + 0x400`이 들어갈 자리가 없으면 상태 9로 접는다.

## 엔드포인트

`wie_backend::local_network::funter`가 이 교환을 아카이브의 `P/` 파일로
답한다. `wie_ktf::emulator`가 `P/`를 벗긴 이름으로 목록을 세워 등록하고,
`socket://211.115.203.17:15102`만 받는다. 조각은 1024바이트씩이다.

받아쓴 것은 `MC_dbOpenDataBase(이름, 4, 1)`로 연 데이터베이스에 그대로
들어간다. 같은 이름의 패키지 파일이 이미 있으면 그 내용으로 버퍼를 채운
채 열리고 타이틀이 앞에서부터 덮어쓰므로, 결과는 같은 바이트다.

**아카이브에 데이터를 넣어두는 것만으로는 건너뛸 수 없다.** 타이틀은 언제나
이 화면을 띄우고, 언제나 접속하고, 목록을 서버에 묻는다. 2(아니오)는 종료다.
