# 드래곤로드 (01038088) 데이터 내려받기

KTF WIPI-C `Clet`, Thumb, 이미지 베이스 `0x100000`이므로
`파일오프셋 = va - 0x100000`. 아카이브는 `P/dragon.*`로 받아둔 데이터를
이미 들고 있다.

## 한 줄 요약

`[드래곤로드 정보] … 데이터파일을 다운받습니다` 화면에서 **1(예)를 눌러도
소켓 하나 열리지 않았다.** 타이틀은 그 전에 net 테이블 **슬롯 34**를 부르고,
답이 음수면 거기서 그만둔다. 우리 테이블은 33칸까지였으므로 그 슬롯은
`-1`로 거절되고 있었다. 밖에서 보이는 것은 `MC_netSetReadCB(0, ...)`가 몇 초에
한 번씩 반복되는 것뿐 — **열린 적이 없는 소켓**에.

## 슬롯 34 (`0x1072b8`)

```asm
1072c4  adds r5, r0      ; r5 = 세션 객체
1072c6  lsls r4, r2,#16  ; (short)r2 = 포트
1072d4  strh r4, [r5,#0x18]
1072d6  ldr  r4, [r3]    ; net 인터페이스 테이블
1072da  adds r3, #0x88   ; 0x88/4 = 34
1072dc  ldr  r0, [r5,#0xc]   ; "kt68wipiwicgs.magicn.com:27090"
1072e0  bl   #0x153668       ; bx r3 트램펄린
1072e4  cmp  r0, #0
1072e6  blt  #0x10731e       ; 음수면 포기 (소켓도 만들지 않는다)
...
1072fa  ldr  r3, [r2]        ; 테이블[0] = MC_netConnect
1072fc  bl   #0x153668       ; connect(콜백, 세션)
107304  cmp  r0, -10 / -7    ; 둘 다 "진행 중"
```

인자는 **r0에 `호스트:포트` 문자열 하나뿐**이다(r1/r2는 직전 호출이 남긴
찌꺼기). 답은 부호만 본다 — 아무것도 보관하지 않는다. 답을 주면 타이틀은
평범한 순서를 밟는다: `MC_netSocket(2, 1)` → 슬롯 30 →
`MC_netSocketConnect(1, 211.115.203.17, 15102)`. 즉 **열기 전에 목적지를 두고
묻는 질문**이지 연결이 아니다. `wie_wipi_c::api::net::check_server` 참고.

문자열 두 개가 바이너리에 나란히 있다 (`0x155030`, `0x155060`).

```
0x155030  kt68wipiwicgs.magicn.com:27091
0x155050  TEST_BILLSOCK
0x155060  kt68wipiwicgs.magicn.com:27090
```

`TEST_BILLSOCK`은 데몬헌터가 슬롯 30에 넘기던 이름과 같은 것이다.

## 프레이밍 (`0x107654`, `0x107770`)

수신은 두 단계다. 헤더 8바이트를 읽고, 그다음 `길이 - 8`을 읽는다.

```asm
1076f4  ldr  r1, [pc,#0x6c]  ; 0x84c  — 매직이 놓이는 자리
1076f8  ldrh r3, [r3]
1076fa  ldr  r2, [pc,#0x6c]  ; 0xffff — 매직 상수
1076fc  cmp  r3, r2
1076fe  bne  ...             ; 아니면 오류 7로 끊는다
10770c  ldr  r3, [pc,#0x5c]  ; 0x848  — 전체 길이가 놓이는 자리
107714  subs r1, #8          ; 본문 = 길이 - 8
```

그러므로 프레임은

```
+0  u32 LE  전체 길이 (헤더 8바이트 포함)
+4  u16     0xffff      ← 반드시 이 값
+6  u16     메시지 id
+8  본문
```

읽기 루프(`0x107770`)는 한 번에 최대 `0x800`바이트씩 슬롯 32로 받아
`객체+0x48` 버퍼에 모으고, 완성된 프레임을 큐에 넣는다(`0x1075e0`).
게임 쪽 층이 그 큐를 꺼내 쓴다.

첫 요청은 20바이트다.

```
14 00 00 00  ff ff 40 06  00 00 02 01  01 01 00 00  00 f0 00 00
└ 길이 20 ┘  └매직┘└id ┘  └──────────── 본문 12 ────────────┘
```

id는 `0x0640` = 1600.

## 지금까지 확인된 것과 남은 것

프로브의 일반 ack 엔드포인트로 이 프레이밍을 그대로 흉내낼 수 있다.

```
WIE_LOCAL_NET_ACK="any,len=u32le,type=4"
```

이러면 `0c000000 ffff4006 00000000`이 돌아가고, 타이틀은 그것을 **정상 프레임으로
받아들인다** — 헤더 8바이트를 읽은 뒤 정확히 4바이트를 더 읽는다. 그리고
`MC_netSetReadCB(1, 0, 0)`으로 콜백을 내리고 세션을 접는다. 즉 프레임 모양은
맞았고 **내용이 아니다.**

남은 것은 메시지 id 층이다: 1600에 대한 진짜 답, 그리고 파일을 실어 나르는
메시지들. 데이터 자체는 아카이브 안에 있으므로(`P/dragon.dat`, `.map`, `.pts`,
`.res`, `.snd`, `.spr`, `.ui`, `.vlu`, `script.csd`, `FUNTER_DL`) 그 층만 읽어내면
로컬에서 그대로 돌려줄 수 있다. `wie_wipi_c::api::database::packaged_store_bytes`가
이미 그 파일들을 `MC_dbOpenDataBase`에 답으로 내주고 있어서, 내려받기가
끝났다고 타이틀이 믿는 순간 읽을 것은 준비되어 있다.

2(아니오)를 고르면 타이틀은 그냥 종료한다. 건너뛸 길은 없다.
