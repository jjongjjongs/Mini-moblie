# 서든어택 포켓 (LGT `0002AC6E`) — 캐쉬 상점 결제 응답

아이템샵에서 캐쉬 아이템을 구매하면 "캐쉬아이템 구매중입니다. 잠시만
기다려주세요..."에서 더 진행되지 않는다.

## 지금까지 확정된 것

### 요청

구매를 확정하면 `BillSocket://121.78.119.73:18009`에 접속해 18바이트를 쓴다.
프레임은 **`u16 BE 명령` + 16바이트 페이로드**로 고정이고, 명령은 두 가지가
연달아 나온다.

```text
29 10 | 00 00 00 0b | "01046119269" 00                            구매
3c 10 | 00 00 00 32 | 00 00 00 32 | 00 00 00 32 | 00 00 00 28     메달 보유량
```

두 번째의 네 값 50/50/50/40은 상점 화면 하단의 메달 4종 보유량 그대로다.

기기 로그에 그대로 찍힌다:

```
billing gateway: 18 bytes [29 10 00 00 00 0b 30 31 30 34 36 31 31 39 32 36 39 00] -> 68 bytes [...]
billing gateway: 18 bytes [3c 10 00 00 00 32 00 00 00 32 00 00 00 32 00 00 00 28] -> the ez-i SDK's own answer, 20 bytes
```

**두 요청은 각각 답을 받아야 한다.** 구매 쪽만 답했을 때 게임은 길이 읽기를
통과해 다음 단계로 갔지만, 메달 쪽이 ez-i 기본 응답을 받아 다시 201MB를
기다렸다 — 화면에서는 "구입합니다 >예"에서 버튼이 안 먹는 것으로 보인다.

### 응답 프레이밍

`SocketInputStream::read()`를 **1바이트씩 네 번** 부른 뒤 그 네 바이트를
**빅엔디안 u32 길이**로 조립하고, 그 길이만큼을 한 번에 읽는다.

```text
[0..4]   u32 BE  본문 길이
[4..]    본문
```

`billing::response()`의 매처 중 `0x2910` 요청의 모양을 아는 것이 없어서
ez-i SDK 기본 응답 20바이트가 나갔고, 게임은 그 앞 4바이트 `0c 00 01 00`을
길이로 읽었다 — 201326848. 로그에 남은 두 줄이 그 결과다:

```
SocketInputStream::read(..., [B, 0,  201326848)
SocketInputStream::read(..., [B, 16, 201326832)
```

오지 않을 201MB를 기다리는 것이 화면에 보이는 그 정지다.

### 판정 문자열

`.text`의 문자열 게터는 `0x1a28`이고, ARM이다(AOT 컴파일된 `SAttack`).

```asm
1a28  mov ip, sp
1a2c  push {fp, ip, lr, pc}
1a30  ldr r3, [pc, #0x34]      ; 0x1a6c -> 0x14001bc  문자열 포인터 표
1a34  mov r2, r0               ; r2 = 인덱스
1a38  ldr r1, [r3, r0, lsl #2]
1a3c  ldr r3, [pc, #0x2c]      ; 0x1a70 -> 0x1400188
1a44  ldr r0, [r3]
1a48  ldr r3, [pc, #0x24]      ; 0x1a74 -> 0x1500000  캐시 기반 주소
1a4c  ldr ip, [pc, #0x24]      ; 0x1a78 -> 0x1405a90  vm_get_constant_string
1a50  add r3, r3, r2, lsl #2
1a54  ldrh r2, [r1]            ; u16 길이
1a58  add r1, r1, #2           ; 문자 배열
1a5c  mov lr, pc
1a60  bx  ip
```

캐시 주소가 `0x1500000 + 인덱스*4`이므로, 이미 찍히고 있는 로그 한 줄이
어떤 문자열을 꺼냈는지 인덱스까지 알려준다:

```
vm_get_constant_string("...") -> 0x..., cached at 0x150038c
```

구매 흐름에서 나오는 인덱스는 이렇다.

| 인덱스 | 캐시 주소 | 문자열 |
|--------|-----------|--------|
| 227 | `0x150038c` | 캐쉬아이템 구매중입니다. |
| 228 | `0x1500390` | 구매 성공 |
| 229 | `0x1500394` | 고객님은 월구매한도로 인해 현재 사용이 불가하니… |
| 230 | `0x1500398` | 구매 실패하였습니다 |

즉 **로그만 봐도 어느 판정에 도달했는지 알 수 있다.**

## 아직 모르는 것 — 본문

응답 본문에 무엇이 들어가야 하는지는 로그 어디에도 없다. 로그에 찍힌 응답은
우리가 만들어 넣은 값이고, 진짜 서버 응답은 남아 있지 않다. 이건 로그의
한계가 아니라 로그에 원래 없는 정보다.

**추측으로 답하지 말 것.** 잘못된 본문은 타이틀을 멈추게 하는 대신 다른
분기로 보내고, 그러면 무엇이 틀렸는지조차 보이지 않는다.

## 프로브 — 파싱 경로를 로그로 끌어내기

`wie_backend::probe`가 이 공백을 메운다. 무장하면 ARM 코어가 **바로 앞
명령에서 흘러들어오지 않은 곳으로 갈 때마다**, 즉 분기할 때마다 그 간선을
기록해서 로그로 내보낸다. 명령 단위가 아니라 분기 단위인 이유는 두 가지다.
직선 코드는 실행량의 대부분이면서 판단에 아무 기여를 하지 않고, 루프는 같은
간선의 반복이라 한 줄과 횟수로 접힌다. 남는 것만으로 기기 로그 버퍼를 넘기지
않고 읽을 수 있다.

`lgt_local_sudden_attack_response()`가 구매 요청을 알아보고,

- 길이 프레이밍이 맞는 응답을 만들어 게임이 길이 읽기를 통과하게 하고,
- 본문은 **구매할 때마다 다른 모양**으로 채우고,
- 그 응답을 게임이 다 읽어간 순간부터 분기 트레이스를 무장한다.

본문 네 가지는 한 번의 기기 실행에서 네 번 구매하면 네 개의 트레이스가
나오도록 돌아가며 쓰인다.

| 라운드 | 본문 |
|--------|------|
| 1 | 전부 0 |
| 2 | 앞 2바이트가 `29 11` (명령 + 1), 나머지 0 |
| 3 | 앞 2바이트가 `29 10` (명령 그대로), 나머지 0 |
| 4 | 각 바이트가 자기 오프셋 값 |

프로브는 하나뿐이고 두 교환은 수십 밀리초 차이로 붙어 있어서, 둘 다 추적하면
뒤엣것이 앞엣것을 끊는다. 그래서 **라운드마다 한 쪽만 번갈아** 추적한다 —
홀수 라운드는 구매, 짝수 라운드는 메달. 네 번 구매하면 명령마다 두 개씩,
서로 다른 본문 모양으로 트레이스가 나온다.

트레이스는 이렇게 찍힌다.

```
probe: tracing the next 30000 branches for 서든어택 포켓 구매 응답 파싱
probe 서든어택 포켓 구매 응답 파싱: 520b4>52130 52148>5216c*7 5216c>521a0 ...
```

`from>to`이고, `*n`은 같은 간선이 연달아 n번 반복됐다는 뜻이다. 주소는 전부
`binary.mod`의 가상주소이므로 `.text`(va `0x1000`, 파일 오프셋 `0x34`)에
그대로 대응한다.

## 기기에서 돌리는 법

1. `claude/zenonia-audio-integration` 브랜치를 이 커밋으로 빌드한다. JIT를
   켜고 빌드해도 된다 — 프로브가 무장된 동안에는 세 엔진 모두 컴파일된 블록을
   건너뛰고 인터프리터로 한 명령씩 도니까 트레이스는 어느 빌드에서나 같다.
2. 서든어택 포켓을 실행해 아이템샵 → 캐쉬 탭으로 들어간다.
3. **아무 캐쉬 아이템이나 네 번 구매를 시도한다.** 매번 "구매중입니다"에서
   결과가 나올 때까지(또는 멈출 때까지) 기다렸다가 다음 시도로 넘어간다.
4. 로그를 저장해서 보낸다.

로그에서 봐야 할 것은 세 가지다.

- `서든어택 포켓: 구매 N, ...` / `서든어택 포켓: 메달 N, ...` — 어떤 본문이
  나갔는지
- `probe ...:` 줄들 — 그 본문으로 게임이 걸어간 경로
- `vm_get_constant_string(...) cached at 0x15003xx` — 어떤 판정에 닿았는지

## 프로브를 걷어낼 때

이건 진단용 비계다. 트레이스가 본문을 확정하면
`lgt_local_sudden_attack_response()`를 진짜 응답으로 바꾸고,
`SUDDEN_ATTACK_PROBE_*`와 `arm_when_drained` 호출을 지운다.
`wie_backend::probe` 자체는 남겨도 된다 — 무장하지 않으면 명령당 완화된
로드 한 번이고, 같은 벽에 막힌 다른 타이틀(영웅서기5, 오셔너스,
훼밀리마트타이쿤)에 그대로 쓸 수 있다.
