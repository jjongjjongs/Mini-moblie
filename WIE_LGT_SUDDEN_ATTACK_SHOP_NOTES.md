# 서든어택 포켓 (LGT `0002AC6E`) — 캐쉬 상점 결제

**해결됨.** 아이템샵에서 캐쉬 아이템을 사면 구매 성공이 뜨고 아이템이 실제로
지급된다.

## 프로토콜

### 요청

구매를 확정하면 `BillSocket://121.78.119.73:18009`에 접속해 18바이트를 쓴다.
프레임은 **`u16 BE 명령` + 16바이트 페이로드**로 고정이고, 명령 두 가지가
연달아 나온다.

```text
29 10 | 00 00 00 0b | "01046119269" 00                            구매
3c 10 | 00 00 00 32 | 00 00 00 32 | 00 00 00 32 | 00 00 00 28     메달 보유량
```

두 번째의 네 값 50/50/50/40은 상점 화면 하단의 메달 4종 보유량 그대로다.
**두 요청 모두 답을 받아야 한다** — 구매 쪽만 답하면 게임은 구매 단계를
통과하고 메달 쪽에서 멈춘다. 화면에서는 "구입합니다 >예"에서 버튼이 안 먹는
것으로 보인다.

### 응답

```text
[0..4]   u32 BE  본문 길이
[4..]    본문
```

게임은 `SocketInputStream::read()`를 1바이트씩 네 번 불러 빅엔디안 u32 길이를
조립하고, 그만큼을 한 번에 읽는다.

이 프레이밍을 모를 때는 ez-i SDK 기본 응답 20바이트가 나갔고, 게임이 그 앞
4바이트 `0c 00 01 00`을 길이로 읽어 201326848바이트를 기다렸다.

### 본문 — 0이면 승인

상점은 `0x5bbdc`의 점프 테이블로 도는 상태 기계다. 응답 대기 상태가 파서를
부르고 필드 하나를 되읽는다.

```asm
5d344  bx  ip                  ; 응답 파서
5d348  ldr r6, [r4, #8]        ; 상점 객체의 필드 배열
5d350  ldr sl, [r6, r3, lsl #2]
5d354  cmp sl, #0
5d358  bne 0x5d7a4             ; 0이 아니면 실패
```

0이면 상태 13으로 떨어져 구매 성공(문자열 228)을 그리고 아이템을 지급한다.
그 외에는 상태 14, 구매 실패하였습니다(문자열 230).

기기 실행으로 확정했다. 본문을 0으로 답한 회차는 화면에 구매 성공이 떴고,
**뒤이은 메달 보고 프레임이 `00 00 00 32`(50)에서 `00 00 00 96`(150)으로
바뀌었다** — 화랑메달셋트가 주는 100개가 실제로 들어간 것이다. 같은 실행에서
0이 아닌 본문 세 가지(`29 11`, `29 10`, 바이트별 오프셋)는 전부 구매
실패하였습니다를 띄웠다.

본문 길이는 타이틀이 말해주지 않는다. 64바이트가 그 실행에서 통과한 값이고,
파서는 판정을 앞쪽에서 읽지 끝까지 세지 않는다.

### 문자열 색인

문자열 게터는 `0x1a28`(ARM)이고, 표는 `0x14001bc`, 캐시는 `0x1500000 +
색인*4`다. 그래서 이미 찍히던 로그 한 줄이 어느 판정에 닿았는지 알려준다.

```
vm_get_constant_string("구매 성공") -> 0x..., cached at 0x1500390
```

| 색인 | 캐시 주소 | 문자열 |
|------|-----------|--------|
| 227 | `0x150038c` | 캐쉬아이템 구매중입니다. |
| 228 | `0x1500390` | 구매 성공 |
| 229 | `0x1500394` | 고객님은 월구매한도로 인해… |
| 230 | `0x1500398` | 구매 실패하였습니다 |

## 이걸 어떻게 알아냈나 — `wie_backend::probe`

로그만으로는 여기까지 올 수 없었다. 로그에 찍힌 응답은 우리가 만들어 넣은
값이고 진짜 서버 응답은 남아 있지 않다. 타이틀이 무엇을 받아들이는지는 그
타이틀 코드 안에만 적혀 있다.

`wie_backend::probe`가 그걸 꺼낸다. 무장하면 ARM 코어가 **바로 앞 명령에서
흘러들어오지 않은 곳으로 갈 때마다** 그 간선을 기록해 로그로 내보낸다.

```
probe 서든어택 포켓 구매 응답 파싱: 9f9ee>5d12c 5d130>5d1e4 5d1f0>1a28 ...
```

명령 단위가 아니라 분기 단위인 이유는 두 가지다. 직선 코드는 실행량의
대부분이면서 판단에 기여하지 않고, 루프는 같은 간선의 반복이라 한 줄과
횟수(`*n`)로 접힌다. 3만 분기가 로그 350KB면 기기 로그 버퍼를 넘기지 않는다.

읽는 법은 간단하다. **본문 모양을 바꿔가며 여러 번 답하고, 트레이스끼리
차집합을 낸다.** 성공한 회차에만 있는 간선과 실패한 회차에만 있는 간선이
갈라지는 지점이 판정이다. 여기서는 `5bbdc>5d7d8`(성공) 대 `5bbdc>5d930`(실패)
한 쌍이 상태 13/14를 가리켰고, 거기서 `0x5d354`의 `cmp sl, #0`까지 두 단계였다.

프로브는 무장하지 않으면 명령당 완화된 로드 한 번이다. JIT·fast 엔진은 무장된
동안에만 컴파일된 블록을 거절하고 인터프리터로 한 명령씩 도니까, 트레이스는
어느 빌드에서나 같다.

같은 벽에 막힌 타이틀 — 영웅서기5(`00032870`), 오셔너스(`0002D6C4`),
훼밀리마트타이쿤 — 에 그대로 쓸 수 있어서 남겨 둔다. 쓰려면 매처에서
`probe::arm_when_drained(라벨, 분기수)`를 부르면 된다. 답이 다 읽힌 순간부터
기록이 시작된다.
