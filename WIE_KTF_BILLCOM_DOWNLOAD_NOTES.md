# billcom 추가 다운로드 5종 (KTF)

`powered by billcom` SDK를 싣고 있는 KTF 타이틀들은 실행 직후
`BillSocket://218.38.12.48`로 "추가 다운로드" 교섭을 한다. 서버는 오래전에
꺼졌고, 우리 쪽은 지금까지 ez-i SDK용 20바이트 대답만 돌려주고 있었으므로
타이틀은 `연결시도중..`에서 영영 멈춰 있었다.

대상:

| AID | 타이틀 | 패키지 태그 |
|---|---|---|
| `010351D5` | 블레이드마스터2 | `BM2_KTF_240` |
| `01034DCD` | 다이어트 타이쿤 | `Diet_KTF_240` |
| `010374F1` | 판타지포에버2 | `FF2_WIPI_176` |
| `01037982` | 열혈고사전설2 | `SCL2_WIPI_240` |
| `0102A356` | 만귀토벌전 | (탭 구분, 아래 참고) |

## 한 줄 요약

- 앞의 넷은 게이트웨이에 **가진 파일 목록**을 보내고 답을 기다린다. 답의
  머리 4바이트(빅엔디언)가 `0`이면 "받을 것 없음"이 되어 그대로 타이틀
  화면으로 들어간다. → `wie_backend::billing::ktf_local_download_response`
- 만귀토벌전은 프로토콜 문제가 아니었다. 이 패키지는 데이터 디렉터리를
  대문자 `P/`가 아니라 **소문자 `p/`**로 싣고 있는데 로더가 `P/`만 벗기고
  있었다. 벗겨주자 타이틀이 `p/*.ed` 표시 파일을 찾아 다운로드 자체를
  건너뛰고 메인 메뉴로 들어간다. 다이어트 타이쿤도 같은 이유로 "가진 것이
  하나도 없다"(전부 크기 0)고 보내고 있었다.

## 요청

ASCII, 구분자는 `|`:

```
14|BM2_KTF_240|main1.png|17084|main2.png|17659|title.png|51687| …
```

파일 수, 패키지 태그, 그리고 (이름, **핸드셋이 가진 크기**) 쌍의 나열이다.
크기는 아카이브 `P/`에 실제로 들어 있는 바이트 수와 정확히 일치하고, 파일을
못 찾으면 `0`으로 나간다 — 소문자 `p/` 버그가 여기서 드러났다.

## 응답 — 실측

SDK는 `read()`를 **두 번** 불러 `(hi << 8) | lo`로 길이를 만들고(빅엔디언
`u16`), 그 길이만큼 버퍼를 채울 때까지 읽는다. 짧게 답하면 영원히 읽는다 —
그게 `연결시도중..`이었다.

블레이드마스터2에 대고 몸통을 바꿔가며 잰 결과:

| 몸통 | 결과 |
|---|---|
| 8바이트 미만 | 계속 읽는다 (`연결시도중..`) |
| `00 00 00 00 00 00 00 00` | **타이틀 화면** |
| 앞 4바이트가 `1`, `2`, `0xffff`, `1000000` | 뒤따를 목록을 기다린다 |
| 앞 4바이트가 `0x41414141` | `현재 단말기내 저장공간이 부족합니다` |
| 앞 4바이트가 `0xffffffff` | 0과 같이 "받을 것 없음" |
| 뒤 4바이트 | 무엇을 넣어도 달라지지 않는다 (읽지 않는다) |

즉 몸통은 최소 8바이트, 머리 `u32`가 **받아올 양**이다. 0이면 받을 것이
없다는 뜻이고, 실제로 없다 — 받았을 데이터는 이미 아카이브의 데이터
디렉터리에 들어 있다. 그래서 답은 `00 08` + 0 여덟 개.

## 만귀토벌전 (`0102A356`)

이쪽만 다른 프로토콜이다. 4바이트 빅엔디언 길이 + 탭 구분:

```
00 00 00 2b   RR \t 7777777777 \t wipi_ktf \t man \t all \t imgCEff_.dat
```

문자열 표에 `KCEN` `UATO` `UATT` `UAFO` `UAFR` `UAFT` `CR1H` 같은 4글자
서비스 타입과 `NC.run():svctype catched = `가 있으니 답도 같은 모양일
것이다. 다만 `p/` 를 벗겨주는 것만으로 이 교섭 자체에 들어가지 않으므로
(파일마다 딸린 1바이트 `.ed` 표시를 찾아 이미 받은 것으로 친다) 프로토콜은
읽지 않았다.

## 확인

다섯 개 모두 타이틀 화면/메인 메뉴까지 들어간다. 블레이드마스터2는 프롤로그
연출(`P/prologe*.png`)까지, 판타지포에버2는 마을 대화까지 확인했다.
